#include<stdio.h>
#include<string.h>
int main()
{
    char a[20];
    int i,j;
    printf("\n enter the number");
    scanf("%s",&a);
    for(i=0;i<=a[i];i++)
    {
        for(j=0;j<=a[i];j++)
        {
         
        }}
            if(a[i]==a[j])
            printf("no");
            else
            printf("yes");
        
    return 0;
}
