#include<stdio.h>
#include<string.h>
int main()
{
    char a[20];
    int i,count,l;
    scanf("%s",&a);
    l=strlen(a);
    for(i=0;a[i]<=l;i++)
    {
       } if(a[i]=='\0')
        
        count++;
        
    printf("%d",++count);
    return 0;
}
