#include<stdio.h>
#include<string.h>
int main()
{
   char a[20];
   int i;
   printf("\n enter the character");
   scanf("%s",&a);
   for(i=0;i<=a[i];i++)
   {
       if(a[i]>='0'&&a[i]<='9')
       
       printf("\n %c",a[i]);
   }
   return 0;
   }

